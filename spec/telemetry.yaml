# yaml-language-server: $schema=schemas/telemetry.schema.json
# Telemetry storage DDL and INSERT statements; SQL syntax is validated by SQLite.
schema: |
  CREATE TABLE IF NOT EXISTS tool_calls (
  	id              INTEGER PRIMARY KEY AUTOINCREMENT,
  	created_at      TEXT    NOT NULL,
  	session_id      TEXT    NOT NULL,
  	ticket_id       TEXT    NOT NULL DEFAULT '',
  	project_name    TEXT    NOT NULL DEFAULT '',
  	working_dir     TEXT    NOT NULL DEFAULT '',
  	agent_id        TEXT    NOT NULL,
  	tool_name       TEXT    NOT NULL,
  	call_type       TEXT    NOT NULL,
  	score           INTEGER CHECK (score IS NULL OR (score BETWEEN 1 AND 5)),
  	note            TEXT    NOT NULL DEFAULT '',
  	exit_code       INTEGER,
  	duration_ms     INTEGER NOT NULL DEFAULT 0 CHECK (duration_ms >= 0),
  	raw_bytes       INTEGER NOT NULL DEFAULT 0 CHECK (raw_bytes >= 0),
  	distilled_bytes INTEGER CHECK (distilled_bytes IS NULL OR distilled_bytes >= 0),
  	output_bytes INTEGER,
  	actual_tokens INTEGER,
  	input_tokens INTEGER,
  	cached_input_tokens INTEGER,
  	output_tokens INTEGER,
  	reasoning_tokens INTEGER,
  	total_tokens INTEGER,
  	potential_savings_tokens INTEGER,
  	potential_savings_bytes INTEGER
  );
  
  CREATE INDEX IF NOT EXISTS idx_tool_calls_session_id  ON tool_calls (session_id);
  CREATE INDEX IF NOT EXISTS idx_tool_calls_ticket_id   ON tool_calls (ticket_id);
  CREATE INDEX IF NOT EXISTS idx_tool_calls_tool_name   ON tool_calls (tool_name);
  CREATE INDEX IF NOT EXISTS idx_tool_calls_agent_id    ON tool_calls (agent_id);
  CREATE INDEX IF NOT EXISTS idx_tool_calls_created_at  ON tool_calls (created_at);
  
  -- note_sanitization_cache backs issue 204's Level 2 (agent-sanitized)
  -- export privacy level: sha256(raw note text) -> LLM-sanitized text, so a
  -- given note is only ever sent to the NoteSanitizer once, no matter how
  -- many export runs reference it. This is purely additive relative to
  -- schemaVersion 2's shape (a new table, no change to tool_calls itself),
  -- so it does not require a schemaVersion bump — CREATE TABLE IF NOT
  -- EXISTS already applies it to a pre-existing database file on next Open.
  CREATE TABLE IF NOT EXISTS note_sanitization_cache (
  	raw_hash    TEXT PRIMARY KEY,
  	clean_text  TEXT NOT NULL,
  	created_at  TEXT NOT NULL
  );
  
  -- note_category_cache backs issue 212's Tier 2 persistent content-hash cache:
  -- sha256(raw note text) -> activity_category enum string, avoiding re-evaluating
  -- seen notes. Purely additive table (no schemaVersion bump needed).
  CREATE TABLE IF NOT EXISTS note_category_cache (
  	raw_hash    TEXT PRIMARY KEY,
  	category    TEXT NOT NULL,
  	created_at  TEXT NOT NULL
  );
  
  -- issue_status_snapshots backs issue 228: an append-only history of
  -- open/closed/draft/unknown ticket counts per project, one row per
  -- 'harnez index' run whose counts differ from that project's most recent
  -- prior row (see InsertIssueSnapshot's dedupe check in issuesnapshot.go --
  -- the dedupe itself is a Go-side read-then-compare, not a DB constraint,
  -- since "identical to the latest row for this project" isn't expressible
  -- as a single-row UNIQUE/CHECK constraint). Purely additive table (no
  -- schemaVersion bump needed), reusing this same telemetry DB rather than a
  -- separate issues/.status-history.jsonl file so all of this repo's SQL/
  -- storage-format ownership stays in one place, per issue 120's "keep SQL
  -- out of the CLI" convention.
  CREATE TABLE IF NOT EXISTS issue_status_snapshots (
  	id            INTEGER PRIMARY KEY AUTOINCREMENT,
  	created_at    TEXT    NOT NULL,
  	project_name  TEXT    NOT NULL,
  	open_count    INTEGER NOT NULL DEFAULT 0 CHECK (open_count >= 0),
  	closed_count  INTEGER NOT NULL DEFAULT 0 CHECK (closed_count >= 0),
  	draft_count   INTEGER NOT NULL DEFAULT 0 CHECK (draft_count >= 0),
  	unknown_count INTEGER NOT NULL DEFAULT 0 CHECK (unknown_count >= 0)
  );
  
  CREATE INDEX IF NOT EXISTS idx_issue_status_snapshots_project     ON issue_status_snapshots (project_name);
  CREATE INDEX IF NOT EXISTS idx_issue_status_snapshots_created_at  ON issue_status_snapshots (created_at);
  
  -- cli_invocations backs issue 326: one row per harnez CLI invocation --
  -- which subcommand ran, when, in which project, by whom, and whether it
  -- succeeded -- written from main() around root.Execute() (cmd/harnez's
  -- executeAndRecord) and read back by harnez log (issue 327). It is
  -- deliberately a separate table from tool_calls rather than a fifth
  -- call_type on it: tool_calls rows are *deliberate* agent-authored
  -- telemetry (a rating, a wrapped shell command) whose aggregates
  -- (GroupStats.FailureCount, UnratedFailureCount, the rate-overhead report)
  -- are tuned around that assumption. Folding an automatic per-invocation
  -- row into the same table would silently change every one of those numbers.
  --
  -- Purely additive table (no schemaVersion bump needed) -- CREATE TABLE IF
  -- NOT EXISTS applies it to a pre-existing database file on next Open, same
  -- reasoning as the three tables above.
  --
  -- args is redacted at write time (see cmd/harnez/clilog.go's redactArgs):
  -- flag names are kept, flag values and unsafe-looking positionals are
  -- replaced with a placeholder. Raw argv is never stored -- it can carry
  -- absolute paths, hostnames (usage --host), and ticket titles.
  CREATE TABLE IF NOT EXISTS cli_invocations (
  	id             INTEGER PRIMARY KEY AUTOINCREMENT,
  	created_at     TEXT    NOT NULL,
  	session_id     TEXT    NOT NULL,
  	agent_id       TEXT    NOT NULL,
  	command        TEXT    NOT NULL,
  	args           TEXT    NOT NULL DEFAULT '',
  	project_name   TEXT    NOT NULL DEFAULT '',
  	working_dir    TEXT    NOT NULL DEFAULT '',
  	ticket_id      TEXT    NOT NULL DEFAULT '',
  	exit_code      INTEGER,
  	duration_ms    INTEGER NOT NULL DEFAULT 0 CHECK (duration_ms >= 0),
  	harnez_version TEXT    NOT NULL DEFAULT ''
  );
  
  CREATE INDEX IF NOT EXISTS idx_cli_invocations_created_at ON cli_invocations (created_at);
  CREATE INDEX IF NOT EXISTS idx_cli_invocations_project    ON cli_invocations (project_name);
  CREATE INDEX IF NOT EXISTS idx_cli_invocations_session_id ON cli_invocations (session_id);
  CREATE INDEX IF NOT EXISTS idx_cli_invocations_command    ON cli_invocations (command);
  
  -- compaction_events stores the provider-visible boundary and optional token
  -- snapshot for Codex PreCompact/PostCompact hooks (issue 423).
  CREATE TABLE IF NOT EXISTS compaction_events (
  	id INTEGER PRIMARY KEY AUTOINCREMENT,
  	created_at TEXT NOT NULL,
  	session_id TEXT NOT NULL,
  	event_type TEXT NOT NULL,
  	turn_id TEXT NOT NULL DEFAULT '',
  	trigger TEXT NOT NULL DEFAULT '',
  	reason TEXT NOT NULL DEFAULT '',
  	input_tokens INTEGER,
  	cached_input_tokens INTEGER,
  	output_tokens INTEGER,
  	reasoning_tokens INTEGER,
  	total_tokens INTEGER,
  	model TEXT NOT NULL DEFAULT ''
  );
  CREATE INDEX IF NOT EXISTS idx_compaction_events_session_id ON compaction_events (session_id);
  CREATE INDEX IF NOT EXISTS idx_compaction_events_created_at ON compaction_events (created_at);
  
  -- session_boundaries records lifecycle and compaction boundaries independently
  -- of tool_calls so sessions remain queryable when no tool call was emitted.
  CREATE TABLE IF NOT EXISTS session_boundaries (
  	id INTEGER PRIMARY KEY AUTOINCREMENT,
  	created_at TEXT NOT NULL,
  	session_id TEXT NOT NULL,
  	boundary_type TEXT NOT NULL,
  	compaction_event_id INTEGER
  );
  CREATE INDEX IF NOT EXISTS idx_session_boundaries_session_id ON session_boundaries (session_id);
  CREATE INDEX IF NOT EXISTS idx_session_boundaries_created_at ON session_boundaries (created_at);
  
  -- token_snapshots preserves every provider snapshot observed around a
  -- boundary. This is append-only so a later tool event cannot overwrite the
  -- compaction or session-exit evidence needed for reconciliation.
  CREATE TABLE IF NOT EXISTS token_snapshots (
  	id INTEGER PRIMARY KEY AUTOINCREMENT,
  	created_at TEXT NOT NULL,
  	session_id TEXT NOT NULL,
  	source TEXT NOT NULL,
  	boundary_id INTEGER,
  	input_tokens INTEGER,
  	cached_input_tokens INTEGER,
  	uncached_input_tokens INTEGER,
  	output_tokens INTEGER,
  	reasoning_tokens INTEGER,
  	total_tokens INTEGER,
  	last_input_tokens INTEGER,
  	last_cached_input_tokens INTEGER,
  	last_output_tokens INTEGER,
  	last_reasoning_tokens INTEGER,
  	last_total_tokens INTEGER
  );
  CREATE INDEX IF NOT EXISTS idx_token_snapshots_session_id ON token_snapshots (session_id);
  CREATE INDEX IF NOT EXISTS idx_token_snapshots_created_at ON token_snapshots (created_at);
  
  -- compaction_economics preserves the exact pricing revision and rates used
  -- for each estimate. Values are integer micro-USD to avoid floating point
  -- drift when pricing or the report is recomputed later.
  CREATE TABLE IF NOT EXISTS compaction_economics (
  	id INTEGER PRIMARY KEY AUTOINCREMENT,
  	created_at TEXT NOT NULL,
  	session_id TEXT NOT NULL,
  	compaction_event_id INTEGER,
  	model TEXT NOT NULL DEFAULT '',
  	pricing_revision TEXT NOT NULL DEFAULT '',
  	cached_input_micros_per_million INTEGER,
  	uncached_input_micros_per_million INTEGER,
  	output_micros_per_million INTEGER,
  	reasoning_micros_per_million INTEGER,
  	status TEXT NOT NULL,
  	compaction_cost_micros INTEGER,
  	post_compaction_cost_micros INTEGER,
  	baseline_cost_micros INTEGER,
  	savings_micros INTEGER,
  	note TEXT NOT NULL DEFAULT ''
  );
  CREATE INDEX IF NOT EXISTS idx_compaction_economics_session_id ON compaction_economics (session_id);
  CREATE INDEX IF NOT EXISTS idx_compaction_economics_created_at ON compaction_economics (created_at);
statements:
  insert_compaction_economics: "INSERT INTO compaction_economics (created_at, session_id, compaction_event_id, model, pricing_revision, cached_input_micros_per_million, uncached_input_micros_per_million, output_micros_per_million, reasoning_micros_per_million, status, compaction_cost_micros, post_compaction_cost_micros, baseline_cost_micros, savings_micros, note) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)"
  insert_compaction_event: "INSERT INTO compaction_events (created_at, session_id, event_type, turn_id, trigger, reason, input_tokens, cached_input_tokens, output_tokens, reasoning_tokens, total_tokens, model) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)"
  insert_session_boundary: "INSERT INTO session_boundaries (created_at, session_id, boundary_type, compaction_event_id) VALUES (?, ?, ?, ?)"
  insert_token_snapshot: "INSERT INTO token_snapshots (created_at, session_id, source, boundary_id, input_tokens, cached_input_tokens, uncached_input_tokens, output_tokens, reasoning_tokens, total_tokens, last_input_tokens, last_cached_input_tokens, last_output_tokens, last_reasoning_tokens, last_total_tokens) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)"
  insert_tool_call: |
    INSERT INTO tool_calls (created_at, session_id, ticket_id, project_name, working_dir, agent_id, tool_name, call_type, score, note, exit_code, duration_ms, raw_bytes, distilled_bytes, output_bytes, actual_tokens, input_tokens, cached_input_tokens, output_tokens, reasoning_tokens, total_tokens, potential_savings_tokens, potential_savings_bytes)
    VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
  insert_cli_invocation: |
    INSERT INTO cli_invocations (created_at, session_id, agent_id, command, args, project_name, working_dir, ticket_id, exit_code, duration_ms, harnez_version)
    VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
  query: |
    SELECT id, created_at, session_id, ticket_id, project_name, working_dir, agent_id, tool_name, call_type, score, note, exit_code, duration_ms, raw_bytes, distilled_bytes, output_bytes, actual_tokens, input_tokens, cached_input_tokens, output_tokens, reasoning_tokens, total_tokens, potential_savings_tokens, potential_savings_bytes FROM tool_calls{{where}} ORDER BY created_at DESC
  aggregate_grouped: |
    SELECT {{group_column}}, COUNT(*), AVG(score), COUNT(score), COUNT(CASE WHEN call_type != ? AND ((exit_code IS NOT NULL AND exit_code != 0) OR (score IS NOT NULL AND score <= 2)) THEN 1 END), COALESCE(SUM(raw_bytes), 0), COALESCE(SUM(distilled_bytes), 0), AVG(duration_ms), AVG(actual_tokens), COALESCE(SUM(potential_savings_tokens), 0), COALESCE(SUM(potential_savings_bytes), 0) FROM tool_calls{{where}} GROUP BY {{group_column}} ORDER BY COUNT(*) DESC, {{group_column}} ASC
  aggregate: |
    SELECT COUNT(*), AVG(score), COUNT(score), COUNT(CASE WHEN exit_code IS NOT NULL AND exit_code != 0 THEN 1 END), COUNT(exit_code), COALESCE(SUM(raw_bytes), 0), COALESCE(SUM(distilled_bytes), 0), AVG(duration_ms) FROM tool_calls{{where}}
  distillation_savings: |
    SELECT COUNT(*), COALESCE(SUM(raw_bytes), 0), COALESCE(SUM(distilled_bytes), 0) FROM tool_calls{{where}}
  unrated_last_rate: 'SELECT MAX(created_at) FROM tool_calls{{where}}'
  unrated_count: 'SELECT COUNT(*) FROM tool_calls{{where}}'
  heartbeat_stats: 'SELECT COUNT(*), MAX(created_at) FROM tool_calls{{where}}'
  heartbeat_calls_since: 'SELECT COUNT(*) FROM tool_calls{{where}}'
# Fetch CLI invocation rows in stable newest-first order.
  query_cli_invocations: |
    SELECT id, created_at, session_id, agent_id, command, args, project_name, working_dir, ticket_id, exit_code, duration_ms, harnez_version FROM cli_invocations{{where}} ORDER BY created_at DESC, id DESC
# Count CLI invocations grouped by command.
  cli_invocation_counts: 'SELECT command, COUNT(*) FROM cli_invocations{{where}} GROUP BY command'
  prune_cli_invocations: 'DELETE FROM cli_invocations WHERE id <= (SELECT MAX(id) - ? FROM cli_invocations)'
predicates:
  tool_name: {sql: 'tool_name = ?'}
  agent_id: {sql: 'agent_id = ?'}
  ticket_id: {sql: 'ticket_id = ?'}
  session_id: {sql: 'session_id = ?'}
  call_type: {sql: 'call_type = ?'}
  project: {sql: 'project_name = ?'}
  since: {sql: 'created_at >= ?'}
  until: {sql: 'created_at < ?'}
group_columns: [tool_name, agent_id, project_name]
quality_checks:
  - name: tool_calls_required_fields
    sql: "SELECT COUNT(*), COALESCE(SUM(CASE WHEN session_id = '' OR agent_id = '' OR tool_name = '' OR call_type = '' THEN 1 ELSE 0 END), 0), CASE WHEN COUNT(*) = 0 THEN 0.0 ELSE 100.0 * SUM(CASE WHEN session_id = '' OR agent_id = '' OR tool_name = '' OR call_type = '' THEN 1 ELSE 0 END) / COUNT(*) END FROM tool_calls"
    description: "Required tool-call identity fields are populated"
    warn_condition: "percentage > 0"
    warn_above_percent: 0
    result: {checked: checked, offending: offending, percentage: percentage}
  - name: cli_invocations_required_fields
    sql: "SELECT COUNT(*), COALESCE(SUM(CASE WHEN session_id = '' OR agent_id = '' OR command = '' THEN 1 ELSE 0 END), 0), CASE WHEN COUNT(*) = 0 THEN 0.0 ELSE 100.0 * SUM(CASE WHEN session_id = '' OR agent_id = '' OR command = '' THEN 1 ELSE 0 END) / COUNT(*) END FROM cli_invocations"
    description: "Required CLI invocation identity fields are populated"
    warn_condition: "percentage > 0"
    warn_above_percent: 0
    result: {checked: checked, offending: offending, percentage: percentage}
  - name: compaction_models_present
    sql: "SELECT COUNT(*), COALESCE(SUM(CASE WHEN model = '' THEN 1 ELSE 0 END), 0), CASE WHEN COUNT(*) = 0 THEN 0.0 ELSE 100.0 * SUM(CASE WHEN model = '' THEN 1 ELSE 0 END) / COUNT(*) END FROM compaction_events"
    description: "Compaction events identify their model"
    warn_condition: "percentage > 0"
    warn_above_percent: 0
    result: {checked: checked, offending: offending, percentage: percentage}
  - name: orphaned_session_boundaries
    sql: "SELECT COUNT(*), COALESCE(SUM(CASE WHEN compaction_event_id IS NOT NULL AND NOT EXISTS (SELECT 1 FROM compaction_events e WHERE e.id = compaction_event_id) THEN 1 ELSE 0 END), 0), CASE WHEN COUNT(*) = 0 THEN 0.0 ELSE 100.0 * SUM(CASE WHEN compaction_event_id IS NOT NULL AND NOT EXISTS (SELECT 1 FROM compaction_events e WHERE e.id = compaction_event_id) THEN 1 ELSE 0 END) / COUNT(*) END FROM session_boundaries"
    description: "Session boundaries reference existing compaction events"
    warn_condition: "percentage > 0"
    warn_above_percent: 0
    result: {checked: checked, offending: offending, percentage: percentage}
  - name: duplicate_compaction_events
    sql: "SELECT COUNT(*), COALESCE(SUM(CASE WHEN id NOT IN (SELECT MIN(id) FROM compaction_events GROUP BY session_id, event_type, turn_id) THEN 1 ELSE 0 END), 0), CASE WHEN COUNT(*) = 0 THEN 0.0 ELSE 100.0 * SUM(CASE WHEN id NOT IN (SELECT MIN(id) FROM compaction_events GROUP BY session_id, event_type, turn_id) THEN 1 ELSE 0 END) / COUNT(*) END FROM compaction_events"
    description: "Compaction event identities are unique"
    warn_condition: "percentage > 0"
    warn_above_percent: 0
    result: {checked: checked, offending: offending, percentage: percentage}
  - name: invalid_tool_call_tokens
    sql: "SELECT COUNT(*), COALESCE(SUM(CASE WHEN input_tokens < 0 OR cached_input_tokens < 0 OR output_tokens < 0 OR reasoning_tokens < 0 OR total_tokens < 0 OR (total_tokens IS NOT NULL AND input_tokens IS NOT NULL AND output_tokens IS NOT NULL AND total_tokens < input_tokens + output_tokens) THEN 1 ELSE 0 END), 0), CASE WHEN COUNT(*) = 0 THEN 0.0 ELSE 100.0 * SUM(CASE WHEN input_tokens < 0 OR cached_input_tokens < 0 OR output_tokens < 0 OR reasoning_tokens < 0 OR total_tokens < 0 OR (total_tokens IS NOT NULL AND input_tokens IS NOT NULL AND output_tokens IS NOT NULL AND total_tokens < input_tokens + output_tokens) THEN 1 ELSE 0 END) / COUNT(*) END FROM tool_calls"
    description: "Tool-call token values are nonnegative and internally plausible"
    warn_condition: "percentage > 0"
    warn_above_percent: 0
    result: {checked: checked, offending: offending, percentage: percentage}
